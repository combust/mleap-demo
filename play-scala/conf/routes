# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# An example controller showing a sample home page
GET     /              			controllers.HomePageController.index()
POST   /upload          		controllers.HomePageController.upload

GET     /model_index             controllers.ModelUploader.index()
POST     /model_file             controllers.ModelUploader.upload()


# Map static resources from the /public folder to the /assets URL path
GET  /assets/*file        		controllers.Assets.at(path="/public", file)
#GET  /images/*file        		controllers.Assets.at(path="/public/images", file)

# Map the webjar static assets to the /webjars URL
GET   /webjars/*file   			controllers.WebJarAssets.at(file)


